import mongoose from 'mongoose'; import dotenv from 'dotenv'; dotenv.config(); import { SchoolAttendance } from './models/schoolAttendance.js'; mongoose.connect(process.env.MONGO_URI || 'mongodb://localhost:27017/KOGI_AGILE_DB_TEST').then(async () => { const explain = await SchoolAttendance.find({ date: { $gte: new Date('2026-07-01'), $lt: new Date('2026-08-01') }, schoolId: '676f4cb12b490484a5fa4172' }).explain(); console.log(explain.executionStats.executionTimeMillis); mongoose.disconnect(); });
